apiVersion: v1
kind: ConfigMap
metadata:
  name: hello-world-config
  labels:
    app: hello-world
    environment: prod
data:
  environment: "prod"
  log_level: "warn"
  app_name: "hello-world"
  version: "v1.0.0"
  debug_enabled: "false" 
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dev-hello-world
  namespace: argocd
  labels:
    app.kubernetes.io/name: hello-world
    app.kubernetes.io/instance: dev
    environment: dev
    managed-by: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/example/argocd-repo-server
    targetRevision: HEAD
    path: config/kustomize/overlays/dev
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
    - PrunePropagationPolicy=foreground
    - PruneLast=true
  revisionHistoryLimit: 10 